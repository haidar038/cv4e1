# ui/

Shared UI primitives for cv4every1. Contains reusable components like buttons, inputs, and layout elements.

## Dependency rules (from architecture-overview.md §5)

- **May depend on:** nothing within src/ (only external UI libraries)
- This module provides base building blocks used by `features/`.