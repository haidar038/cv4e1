# features/

UI components grouped by feature for cv4every1.

## Dependency rules (from architecture-overview.md §5)

- **May depend on:** all other src/ modules (`core/`, `storage/`, `render/`, `ai/`, `content/`, `ui/`)
- This is the composition layer where React components live.